# Local-only variables for development. Never commit real credentials.
OPENAI_MODEL=gpt-4.1-mini
SESSION_TTL_SECONDS=28800
MAX_UPLOAD_BYTES=10485760

# Set BOOTSTRAP_TOKEN in .dev.vars or with `wrangler secret put`.
BOOTSTRAP_TOKEN=replace-with-a-long-random-value
