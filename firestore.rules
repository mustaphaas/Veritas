rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Veritas data is available only through the authenticated Cloud Function API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
